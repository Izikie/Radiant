package net.radiant.audio;

public class OggInfo {
    final int channels;
    final int sampleRate;

    public OggInfo(int channels, int sampleRate) {
        this.channels = channels;
        this.sampleRate = sampleRate;
    }
}